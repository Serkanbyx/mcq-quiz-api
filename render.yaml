services:
  - type: web
    name: mcq-quiz-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run seed && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_PATH
        value: ./data/quiz.db
